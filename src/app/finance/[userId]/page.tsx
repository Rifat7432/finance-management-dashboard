import SpendingOverview from '@/components/layout/spending';
import React from 'react';

const page = () => {
    return (
        <div>
            <SpendingOverview/>
        </div>
    );
};

export default page;